# PRD — storytowebtoon 가이드 챗봇 (도구 설계 관점)

**작성일**: 2026-08-27
**근거**: AIFFEL N026(도구 설계) 챕터7 과제

## 1. 배경

`storytowebtoon` Phase 1은 웹소설 시놉시스/원고를 입력하면 시나리오 JSON을 추출하고, 4인 전문가 페르소나(작가·PD·플랫폼관계자·독자)가 병렬로 평가해 연재 추천 등급을 매기는 파이프라인이다. 현재는 사람이 Streamlit UI 버튼을 순서대로 눌러 진행하는 결정론적 흐름이며, 에이전트가 도구를 스스로 선택하지 않는다.

## 2. 목표

이 파이프라인의 핵심 함수 4개(`extract_synopsis`, `extract_scenario`, `evaluate_personas`, `generate_report`)를 "도구 계약"으로 공식화해:

1. 지금 — 각 함수의 계약을 명문화해 코드에 이미 존재하는 빈틈(오류 처리 미흡, 죽은 필드 등)을 드러낸다.
2. 나중 — 이 시스템에 자연어 인터페이스나 에이전트 오케스트레이션을 얹을 때, 이 계약이 하네스의 뼈대가 된다.

## 3. 대상 사용자

이 프로젝트를 운영하는 개발자/기획자 본인. 로컬 환경에서만 실행되며, 아직 외부 사용자에게 노출되는 서비스가 아니다.

## 4. 범위

### In Scope
- `extract_synopsis`, `extract_scenario`, `evaluate_personas`, `generate_report` 4개 도구의 계약 설계
- 권한/승인 규칙, 오류·보류·이관 규칙, 반환 형식 규약
- 테스트 문항(정상·경계·오류·공격) 8개 및 공격 시도 1건 이상의 실제 API 실증
- 트레이스 양식 및 실제 실행 기록

### Out of Scope
- 이미지 생성·숏폼 영상·번역 (storytowebtoon Phase 2~4)
- 실제 코드 리팩터링(개선 과제로만 기록, 이번 제출에서 구현하지 않음)
- 다중 사용자·인증·과금 등 프로덕션 운영 기능

## 5. 성공 기준

- [x] 도구 4개 전부 정의서 6항목(이름/하는 일/쓰는 때·안 쓰는 때/인자 스키마/반환 형식/실패 뒤 행동) 완비
- [x] 테스트 문항에 공격 시도 1개 이상 + 기대 동작 기록
- [x] 공격 시도 최소 1건은 실제 API 호출로 실증 (모의가 아닌 실제 검증)
- [x] 회귀 확인 절차와 4대 안티패턴 자가점검 포함

## 6. 다음 단계

`design-packet.md`의 "개선 과제" 섹션에 기록된 6개 항목(300,000자 게이트, 씬/컷 검증, 부분실패 보존 등)을 실제 코드에 반영하는 것은 이번 제출 범위 밖이며, 별도 세션에서 진행한다.
